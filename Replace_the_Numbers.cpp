#include <bits/stdc++.h>
using namespace std;
int mem[500001];
int main(){
    // freopen("data.txt","r",stdin);
    for(int i=0;i<=500000;i++)
        mem[i]=i;
    vector <int> A;
    int queries;
    cin>>queries;
    for(int i=0;i<queries;i++){
        
    }
    for(int i=0;i<A.size();i++)
        cout<<A[i]<<" ";
    return 0;
}